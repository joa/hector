package hector.xml.emitter

import hector.util.{TextOutput, trimToOption}

import javax.annotation.Nullable
import javax.annotation.concurrent.ThreadSafe

import scala.xml._

@ThreadSafe
object XmlEmitter {
  def toString(node: Node, stripComments: Boolean = false, trim: Boolean = false, humanReadable: Boolean = false): String = { "TODO"
  }
}
