package hector.http

/**
 */
trait HttpRequestInput {
  //TODO(joa): this is going to be tricky. need to have an actor with the request that sits on the front-end node and this should be a ref to that actor.
}
